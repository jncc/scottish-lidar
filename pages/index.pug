
extends ../layout.pug

block head
  title Scottish Remote Data Sensing Portal - Home

block content
  h1 Home page
  - var pets = ['cat', 'dog']
  each petName in pets
    p #{petName}
  div
    a(href='/data' + process.env.PAGE_EXTENSION) Go to app
block scripts
  script(src='../bundles/index.ts')
