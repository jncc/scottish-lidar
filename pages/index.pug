
extends ../templates/layout.pug

block variables
    - var page_name = 'index'

block head
  title Scottish Remote Data Sensing Portal - Home

block content

  div.intro
    section.header-content
      h1.header-title Find, share and reuse 
        h3.header-subtitle remote sensed data provided by Scottish public sector organisations
        a.btn.btn-dark.mr-1(href='/data' + process.env.PAGE_EXTENSION + '#list')
          | Browse datasets
        a.btn.btn-primary.ml-1(href='/data' + process.env.PAGE_EXTENSION + '#map')
          | Explore the map
          
  include ../templates/footer

block scripts
  script(src='../bundles/index.ts')
