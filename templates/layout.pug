block variables
doctype html
html(lang='en')
  head
    meta(name='x-version' content='x-version-stamp')
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    block meta
    link(rel='shortcut icon' href='../assets/favicon.ico')
    link(href='https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,700,900,400italic' rel='stylesheet')
    link(href="https://fonts.googleapis.com/css?family=Hind+Madurai" rel="stylesheet")
    <script src="https://cc.cdn.civiccomputing.com/9/cookieControl-9.x.min.js" type="text/javascript"></script>
    block head
      title Scottish Remote Sensing Portal | Scottish Government

    //- 
      Clickjacking vulnerability fix below
      Github pages doesn't allow changing response headers, so use this script instead
      https://github.com/OWASP/CheatSheetSeries/blob/master/cheatsheets/Clickjacking_Defense_Cheat_Sheet.md#best-for-now-legacy-browser-frame-breaking-script
      Parcel also doesn't like <style> tags so pull the styling in from a css file
    link(href="antiClickjack.css" rel="stylesheet" id="antiClickjack")
    script(type="text/javascript").
      if (self === top) {
        var antiClickjack = document.getElementById("antiClickjack");
        antiClickjack.parentNode.removeChild(antiClickjack);
      } else {
        top.location = self.location;
      }
  body
    div#layout-container
      a(class='accessibility', href='#main') Skip to content
      include ./header
      //- uncomment to include the announcement banner
      //- include ./announcement
      block content
      a(class='accessibility', href='#layout-container') Back to top
    block scripts
    if process.env.COOKIE_CONSENT_KEY
      include ./cookie-consent-script
    
